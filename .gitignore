node_modules/
dist/
.env
# local runtime state & testnet SEEDS — DO NOT COMMIT
.wallets.json
.agents.json
.dossiers.json
.payments.json
.loans.json
web/node_modules/
web/dist/
*.log
.DS_Store
# SDD scratch (briefs, reports, review packages, progress ledger)
.superpowers/
# dev-only artifacts — kept locally, excluded from the clean public repo
docs/
legacy/
spike-*.ts
smoke-pivot.ts
smoke-tierb.ts
smoke.mjs
setup-demo-wallet.ts
# AML sanctions snapshot — data/sanctions_snapshot_20260621.csv (~7MB, OFAC SDN, public-domain
# source) and the small data/sanctions_fixture.json are BOTH committed, so the repo is
# clone-and-run with real screening. See data/SOURCE.md. Refresh the snapshot with:
#   curl -L -o data/sanctions_snapshot_20260621.csv \
#     "https://data.opensanctions.org/datasets/latest/us_ofac_sdn/targets.simple.csv"
# Runtime-generated World ID nullifier store (replay prevention log)
data/worldid-nullifiers.json
# Thick-file agent wallet (pre-seeded by scripts/seed-thick-agent.ts)
# Contains a testnet seed — never commit.
.thick-agent.json
